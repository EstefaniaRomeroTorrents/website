// AlertButton.js
function AlertButton() {
  return (
    <div>
      <button onClick={() => alert("Alert Button Clicked!")}>
        Alert Button
      </button>
    </div>
  );
}
export default AlertButton;
